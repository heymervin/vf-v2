// Placeholder type — run `pnpm db:types` to regenerate from your live Supabase schema.
// That script writes the full Database type definition to this file.
export type Database = Record<string, never>;
